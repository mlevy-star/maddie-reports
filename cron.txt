# Weekly Publisher Health Report — Daily 10:00 AM
# Run via Claude Code CLI (requires Hex, Slack, Gmail MCPs configured)
#
# To install: crontab -e, then paste the line below
# (adjust path to match your Claude Code installation)

0 10 * * * cd /home/user/maddie-reports && claude -p "Run the weekly publisher health report. Use the config in weekly_publisher_health_report.py. Pull data from Hex (Supply Performance dashboard), compute RPL by page type WoW for all publisher segments (All Pubs excl. MB/GP, Gopuff/BevMo, Mindbody) and network-wide advertiser signals, then send to Slack channel C0AV8GH3EQ5 and email mlevy@disconetwork.com." --allowedTools "mcp__Hex__*,mcp__Slack__*,mcp__Gmail__*,Bash"
