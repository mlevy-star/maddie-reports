# Weekly Publisher Health Report — Mon–Fri 9:00 AM ET
# Run via Claude Code CLI (requires Hex, Slack, Gmail MCPs configured)
#
# To install: crontab -e, then paste the line below
# (adjust path to match your Claude Code installation)

0 9 * * 1-5 cd /home/user/maddie-reports && claude -p "Run the weekly publisher health report for the last 7 days. Use the config in weekly_publisher_health_report.py. Pull data from Hex (Publisher Alerts + Supply Performance), apply health flags, send to Slack channel C0AV8GH3EQ5 and email mlevy@disconetwork.com. Two important rules: (1) In the RPM Summary flagging section, only include publishers with at least RPM_FLAG_MIN_DISPLAYS (5000) brand displays in the current week — smaller publishers are too low-volume for their RPM swings to be meaningful. All publishers above RPM_MIN_DISPLAYS (100) should still appear in the Full Account Breakdown table but without health flags. (2) Always open the report with a plain-language TL;DR paragraph (2-4 sentences) that names the biggest story of the week, the primary advertiser drivers behind it, and any meaningful bright spots — written so someone reading only that paragraph gets the gist." --allowedTools "mcp__Hex__*,mcp__Slack__*,mcp__Gmail__*,Bash"
