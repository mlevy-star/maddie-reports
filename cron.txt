# Weekly Publisher Health Report — every day at 9:00 AM ET
# Run via Claude Code CLI (requires Hex, Slack, Gmail MCPs configured)
#
# To install:
#   crontab -e
#   Paste the TZ line and the cron entry below, then save.

TZ=America/New_York
0 9 * * * /home/user/maddie-reports/run_report.sh >> /home/user/maddie-reports/report.log 2>&1
