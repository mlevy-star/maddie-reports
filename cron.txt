# Weekly Publisher Health Report — Mon–Fri 9:00 AM ET
# Run via Claude Code CLI (requires Hex, Slack, Gmail MCPs configured)
#
# To install: crontab -e, then paste the line below
# (adjust path to match your Claude Code installation)

0 9 * * 1-5 cd /home/user/maddie-reports && claude -p "Run the weekly publisher health report for the last 7 days. Use the config in weekly_publisher_health_report.py. Pull RPL data only from the Supply Performance dashboard (hex_supply_performance_url) — do NOT pull RPM or use the Publisher Alerts dashboard. Apply health flags to RPL changes by page type. Send to Slack channel C0AV8GH3EQ5 and email mlevy@disconetwork.com. Always open the report with a plain-language TL;DR paragraph (2-4 sentences) that names the biggest story of the week, the primary advertiser drivers behind it, and any meaningful bright spots — written so someone reading only that paragraph gets the gist. In the network-wide advertiser signals section, always include the total WoW spend delta in dollars for each advertiser (e.g. FreeShipping.com: −$6,893 across 17 publishers) so the magnitude of each shift is immediately visible." --allowedTools "mcp__Hex__*,mcp__Slack__*,mcp__Gmail__*,Bash"
